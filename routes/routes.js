const express = require('express');
const app = express();

const userRouter = express.Router();


module.exports = userRouter;
// exports.userRouter = { pagination , userRouter , searchRoute , delimited , ajax , jsonplaceholder , jstask , register , result , attendance , dynamicgrid ,pagination  };
